import { Title } from '@mantine/core';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Kemda - Contact',
  description: 'Kemda Contructions BV - Contact pagina',
};

export default function Contact() {
  return (
    <div>
      <Title>Contact</Title>
    </div>
  );
}

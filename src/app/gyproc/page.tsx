import { Title } from '@mantine/core';
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Kemda - Gyproc',
  description: 'Kemda Contructions BV - Gyproc pagina',
};

export default function Gyproc() {
  return (
    <div>
      <Title>Gyproc</Title>
    </div>
  );
}
